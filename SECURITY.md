# Security

This is a public, read-only research site. Never add provider tokens, API keys,
broker credentials, personal portfolio records or private user information to
the source tree or `public/data/portfolio.json`.

If a credential is committed, rotate it immediately and remove it from Git
history before publishing again.
